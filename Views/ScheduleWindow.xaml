<Window x:Class="Kiosk.Views.ScheduleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Расписание уроков"
        WindowState="Maximized"
        WindowStyle="None"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <!-- Поддержка эмодзи -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Emoji, Segoe UI, sans-serif"/>
        </Style>


        <Style x:Key="DayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1A3A5C"/>
            <Setter Property="Foreground" Value="#8AADD4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Padding" Value="28,16"/>
            <Setter Property="MinWidth" Value="90"/>
            <Setter Property="MinHeight" Value="60"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#254780"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A5298"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BackButtonStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="14"
                                Background="#1E3A5F" Padding="24,14">
                            <Border.Effect>
                                <DropShadowEffect Color="#000" Opacity="0.3" BlurRadius="10" ShadowDepth="3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A5298"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#254780"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0D1B2A" Offset="0"/>
                <GradientStop Color="#1A2F4A" Offset="0.5"/>
                <GradientStop Color="#0A1628" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ШАПКА -->
        <Border Grid.Row="0" Padding="28,16">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#0D1F35" Offset="0"/>
                    <GradientStop Color="#122840" Offset="0.5"/>
                    <GradientStop Color="#0D1F35" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect Color="#000" Opacity="0.5" BlurRadius="20" ShadowDepth="4" Direction="270"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="Расписание уроков"
                               Foreground="White" FontSize="32" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="WeekTypeText" Text="Текущая неделя"
                                   Foreground="#4FC3F7" FontSize="16" Margin="0,4,0,0"/>
                        <TextBlock x:Name="UpdateTimeText" Text=""
                                   Foreground="#4A6A8A" FontSize="16" Margin="0,4,0,0"/>
                    </StackPanel>
                </StackPanel>

                <!-- Выбор класса -->
                <Border Grid.Column="1" Margin="0,0,16,0" CornerRadius="12"
                        Padding="16,10" BorderThickness="1.5">
                    <Border.Background>
                        <SolidColorBrush Color="#0F2341"/>
                    </Border.Background>
                    <Border.BorderBrush>
                        <SolidColorBrush Color="#1E4DB7"/>
                    </Border.BorderBrush>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Класс:" Foreground="#8AADD4"
                                   FontSize="18" FontWeight="SemiBold"
                                   VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <ComboBox x:Name="ClassComboBox"
                                  Width="200" MinHeight="46"
                                  FontSize="20" Foreground="Black"
                                  Background="White"
                                  BorderThickness="0"
                                  SelectionChanged="ClassComboBox_SelectionChanged"/>
                    </StackPanel>
                </Border>

                <!-- Кнопка назад -->
                <Button Grid.Column="2" Style="{StaticResource BackButtonStyle}"
                        Click="BackButton_Click">
                    <TextBlock Text="← На главную" Foreground="White"
                               FontSize="18" FontWeight="Bold"/>
                </Button>
            </Grid>
        </Border>

        <!-- Кнопки дней недели -->
        <Border Grid.Row="2" Padding="20,12">
            <Border.Background>
                <SolidColorBrush Color="#0F2341"/>
            </Border.Background>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="MondayButton" Content="ПН"
                        Click="DayButton_Click" Style="{StaticResource DayButtonStyle}"/>
                <Button x:Name="TuesdayButton" Content="ВТ"
                        Click="DayButton_Click" Style="{StaticResource DayButtonStyle}"/>
                <Button x:Name="WednesdayButton" Content="СР"
                        Click="DayButton_Click" Style="{StaticResource DayButtonStyle}"/>
                <Button x:Name="ThursdayButton" Content="ЧТ"
                        Click="DayButton_Click" Style="{StaticResource DayButtonStyle}"/>
                <Button x:Name="FridayButton" Content="ПТ"
                        Click="DayButton_Click" Style="{StaticResource DayButtonStyle}"/>
                <Button x:Name="SaturdayButton" Content="СБ"
                        Click="DayButton_Click" Style="{StaticResource DayButtonStyle}"/>
            </StackPanel>
        </Border>

        <!-- Контент расписания -->
        <Border Grid.Row="3" Margin="16,12,16,8" CornerRadius="18"
                BorderThickness="1.5">
            <Border.Background>
                <SolidColorBrush Color="#0F2341"/>
            </Border.Background>
            <Border.BorderBrush>
                <SolidColorBrush Color="#1E4DB7"/>
            </Border.BorderBrush>
            <Border.Effect>
                <DropShadowEffect Color="#000" Opacity="0.3" BlurRadius="16" ShadowDepth="0"/>
            </Border.Effect>
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          PanningMode="VerticalOnly" Padding="6">
                <StackPanel x:Name="ScheduleContainer" Margin="16">
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Нет уроков -->
        <Border Grid.Row="3" x:Name="NoLessonsContainer"
                Margin="16,12,16,8" CornerRadius="18" BorderThickness="1.5"
                Visibility="Collapsed">
            <Border.Background>
                <SolidColorBrush Color="#0F2341"/>
            </Border.Background>
            <Border.BorderBrush>
                <SolidColorBrush Color="#1E4DB7"/>
            </Border.BorderBrush>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="📭" FontSize="64" TextAlignment="Center" Margin="0,0,0,20"/>
                <TextBlock x:Name="NoLessonsText"
                           Text="На выбранный день уроков нет"
                           FontSize="24" Foreground="#4A6A8A"
                           HorizontalAlignment="Center" FontWeight="SemiBold"/>
                <TextBlock Text="Выберите другой день"
                           FontSize="18" Foreground="#2A4A6A"
                           HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <!-- Статусбар -->
        <Border Grid.Row="4" Padding="28,10" Background="#0A1628">
            <Grid>
                <TextBlock x:Name="StatusText" Text="Готово"
                           Foreground="#4A6A8A" FontSize="14" VerticalAlignment="Center"/>
                <TextBlock Text="© RENDERGAMES • Школьный киоск"
                           Foreground="#2A4A6A" FontSize="13"
                           VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

    </Grid>
</Window>
