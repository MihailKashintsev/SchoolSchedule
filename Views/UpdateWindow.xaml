<Window x:Class="Kiosk.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Доступно обновление"
        Width="560" Height="420"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Emoji, Segoe UI, sans-serif"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" BorderThickness="1.5" ClipToBounds="True">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#0D1B2A" Offset="0"/>
                <GradientStop Color="#0F2341" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.BorderBrush>
            <SolidColorBrush Color="#1E4DB7"/>
        </Border.BorderBrush>
        <Border.Effect>
            <DropShadowEffect Color="#000" Opacity="0.6" BlurRadius="30" ShadowDepth="0"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Шапка -->
            <Border Grid.Row="0" CornerRadius="20,20,0,0" Padding="28,20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#0D1F35" Offset="0"/>
                        <GradientStop Color="#122840" Offset="0.5"/>
                        <GradientStop Color="#0D1F35" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🚀" FontSize="28" VerticalAlignment="Center" Margin="0,0,14,0"/>
                    <StackPanel>
                        <TextBlock Text="Доступно обновление!" FontSize="22"
                                   FontWeight="Bold" Foreground="White"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock x:Name="CurrentVersionText" FontSize="14"
                                       Foreground="#4A6A8A"/>
                            <TextBlock Text=" → " FontSize="14" Foreground="#4A6A8A"/>
                            <TextBlock x:Name="LatestVersionText" FontSize="14"
                                       Foreground="#4FC3F7" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Описание релиза -->
            <Border Grid.Row="1" Margin="20,16,20,0" CornerRadius="14"
                    BorderThickness="1" Padding="18">
                <Border.Background>
                    <SolidColorBrush Color="#0A1628"/>
                </Border.Background>
                <Border.BorderBrush>
                    <SolidColorBrush Color="#1E4DB7"/>
                </Border.BorderBrush>
                <ScrollViewer VerticalScrollBarVisibility="Auto" PanningMode="VerticalOnly">
                    <TextBlock x:Name="ReleaseNotesText"
                               FontSize="14" Foreground="#C8D8EA"
                               TextWrapping="Wrap" LineHeight="22"/>
                </ScrollViewer>
            </Border>

            <!-- Прогресс-бар -->
            <Border Grid.Row="2" Margin="20,12,20,0" CornerRadius="10"
                    x:Name="ProgressPanel" Visibility="Collapsed" Padding="12,10">
                <Border.Background>
                    <SolidColorBrush Color="#0A1628"/>
                </Border.Background>
                <StackPanel>
                    <TextBlock x:Name="ProgressLabel" Text="Скачивание..."
                               FontSize="13" Foreground="#8AADD4" Margin="0,0,0,6"/>
                    <ProgressBar x:Name="ProgressBar" Height="10" Minimum="0" Maximum="100"
                                 Background="#0F2341" Foreground="#4FC3F7">
                        <ProgressBar.Template>
                            <ControlTemplate TargetType="ProgressBar">
                                <Border CornerRadius="5" Background="#0F2341"
                                        BorderBrush="#1E4DB7" BorderThickness="1">
                                    <Border x:Name="PART_Indicator" HorizontalAlignment="Left"
                                            CornerRadius="5">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                <GradientStop Color="#1E4DB7" Offset="0"/>
                                                <GradientStop Color="#4FC3F7" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                    </Border>
                                </Border>
                            </ControlTemplate>
                        </ProgressBar.Template>
                    </ProgressBar>
                </StackPanel>
            </Border>

            <!-- Кнопки -->
            <Grid Grid.Row="3" Margin="20,14,20,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Установить -->
                <Button Grid.Column="0" x:Name="InstallButton"
                        Height="48" Cursor="Hand" BorderThickness="0"
                        Click="InstallButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" CornerRadius="12">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#1E4DB7" Offset="0"/>
                                        <GradientStop Color="#3B82F6" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="b" Property="Opacity" Value="0.8"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Opacity" Value="0.88"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⬇ " FontSize="16" Foreground="White" VerticalAlignment="Center"/>
                        <TextBlock Text="Установить" FontSize="15" FontWeight="Bold"
                                   Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- Скачать ZIP -->
                <Button Grid.Column="2" x:Name="DownloadZipButton"
                        Height="48" Cursor="Hand" BorderThickness="0"
                        Click="DownloadZipButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" CornerRadius="12" Background="#0F2341"
                                    BorderBrush="#1E4DB7" BorderThickness="1.5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="b" Property="Opacity" Value="0.8"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="#122840"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <TextBlock Text="📦 Скачать ZIP" FontSize="15"
                               Foreground="#8AADD4" FontWeight="Bold" VerticalAlignment="Center"/>
                </Button>

                <!-- Пропустить -->
                <Button Grid.Column="4" x:Name="SkipButton"
                        Height="48" Cursor="Hand" BorderThickness="0"
                        Click="SkipButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" CornerRadius="12" Background="Transparent">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="#0A1628"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <TextBlock Text="Пропустить" FontSize="15"
                               Foreground="#4A6A8A" VerticalAlignment="Center"/>
                </Button>

            </Grid>
        </Grid>
    </Border>
</Window>
