<Window x:Class="Kiosk.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="О проекте"
        Icon="pack://application:,,,/icon.ico"
        WindowState="Maximized"
        WindowStyle="None"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <!-- Поддержка эмодзи -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Emoji, Segoe UI, sans-serif"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0D1B2A" Offset="0"/>
                <GradientStop Color="#1A2F4A" Offset="0.5"/>
                <GradientStop Color="#0A1628" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ШАПКА -->
        <Border Grid.Row="0" Padding="28,16">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#0D1F35" Offset="0"/>
                    <GradientStop Color="#122840" Offset="0.5"/>
                    <GradientStop Color="#0D1F35" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect Color="#000" Opacity="0.5" BlurRadius="20" ShadowDepth="4" Direction="270"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="О проекте"
                           Foreground="White" FontSize="32" FontWeight="Bold"
                           VerticalAlignment="Center"/>

                <!-- Кнопка назад -->
                <Button Grid.Column="1" Click="BackButton_Click" Cursor="Hand"
                        BorderThickness="0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" CornerRadius="14"
                                    Background="#1E3A5F" Padding="24,14">
                                <Border.Effect>
                                    <DropShadowEffect Color="#000" Opacity="0.3" BlurRadius="10" ShadowDepth="3"/>
                                </Border.Effect>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#2A5298"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#254780"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <TextBlock Text="← На главную" Foreground="White"
                               FontSize="18" FontWeight="Bold"/>
                </Button>
            </Grid>
        </Border>

        <!-- Контент -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      PanningMode="VerticalOnly" Padding="0,0,5,0">
            <StackPanel Margin="60,30,60,30" HorizontalAlignment="Center" MaxWidth="900">

                <!-- Иконка и название -->
                <Border Width="120" Height="120" CornerRadius="24"
                        HorizontalAlignment="Center" Margin="0,0,0,24">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#1E4DB7" Offset="0"/>
                            <GradientStop Color="#3B82F6" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#1E4DB7" Opacity="0.5" BlurRadius="20" ShadowDepth="0"/>
                    </Border.Effect>
                    <Image Source="pack://application:,,,/Images/AIhelper.png"
                           Stretch="Uniform" Margin="16"/>
                </Border>

                <TextBlock Text="Школьный информационный киоск"
                           FontSize="28" FontWeight="Bold" Foreground="White"
                           HorizontalAlignment="Center" Margin="0,0,0,12"/>

                <Border Background="#0F2341" CornerRadius="20" Padding="20,10"
                        HorizontalAlignment="Center" Margin="0,0,0,36"
                        BorderThickness="1.5">
                    <Border.BorderBrush>
                        <SolidColorBrush Color="#1E4DB7"/>
                    </Border.BorderBrush>
                    <TextBlock Text="Версия 1.0.0" FontSize="18"
                               Foreground="#8AADD4" HorizontalAlignment="Center"/>
                </Border>

                <!-- QR-коды -->
                <TextBlock Text="Полезные ссылки" FontSize="22" FontWeight="Bold"
                           Foreground="#4FC3F7" HorizontalAlignment="Center"
                           Margin="0,0,0,24"/>

                <Grid Margin="0,0,0,36">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- QR 1 -->
                    <Border Grid.Column="0" Margin="0,0,16,0" CornerRadius="18"
                            Padding="28" BorderThickness="1.5">
                        <Border.Background>
                            <SolidColorBrush Color="#0F2341"/>
                        </Border.Background>
                        <Border.BorderBrush>
                            <SolidColorBrush Color="#1E4DB7"/>
                        </Border.BorderBrush>
                        <Border.Effect>
                            <DropShadowEffect Color="#000" Opacity="0.3" BlurRadius="16" ShadowDepth="0"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="Сайт команды" FontSize="20" FontWeight="Bold"
                                       Foreground="White" HorizontalAlignment="Center"
                                       Margin="0,0,0,20"/>
                            <Border Width="200" Height="200" CornerRadius="12"
                                    Background="White" HorizontalAlignment="Center"
                                    Margin="0,0,0,20" Padding="8">
                                <Image x:Name="QrCode1Image" Stretch="Uniform"/>
                            </Border>
                            <Border Background="#122840" CornerRadius="10" Padding="14,10"
                                    BorderThickness="1.5" Cursor="Hand"
                                    MouseLeftButtonDown="QrCodeText_MouseLeftButtonDown">
                                <Border.BorderBrush>
                                    <SolidColorBrush Color="#2A5298"/>
                                </Border.BorderBrush>
                                <TextBlock x:Name="QrCode1Text" Text="https://rendergames.tilda.ws/"
                                           FontSize="15" Foreground="#4FC3F7"
                                           HorizontalAlignment="Center"
                                           TextWrapping="Wrap" TextAlignment="Center"/>
                            </Border>
                            <TextBlock Text="нажмите для копирования" FontSize="13"
                                       Foreground="#4A6A8A" HorizontalAlignment="Center"
                                       Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- QR 2 -->
                    <Border Grid.Column="1" Margin="16,0,0,0" CornerRadius="18"
                            Padding="28" BorderThickness="1.5">
                        <Border.Background>
                            <SolidColorBrush Color="#0F2341"/>
                        </Border.Background>
                        <Border.BorderBrush>
                            <SolidColorBrush Color="#1E4DB7"/>
                        </Border.BorderBrush>
                        <Border.Effect>
                            <DropShadowEffect Color="#000" Opacity="0.3" BlurRadius="16" ShadowDepth="0"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="Сайт школы" FontSize="20" FontWeight="Bold"
                                       Foreground="White" HorizontalAlignment="Center"
                                       Margin="0,0,0,20"/>
                            <Border Width="200" Height="200" CornerRadius="12"
                                    Background="White" HorizontalAlignment="Center"
                                    Margin="0,0,0,20" Padding="8">
                                <Image x:Name="QrCode2Image" Stretch="Uniform"/>
                            </Border>
                            <Border Background="#122840" CornerRadius="10" Padding="14,10"
                                    BorderThickness="1.5" Cursor="Hand"
                                    MouseLeftButtonDown="QrCodeText_MouseLeftButtonDown">
                                <Border.BorderBrush>
                                    <SolidColorBrush Color="#2A5298"/>
                                </Border.BorderBrush>
                                <TextBlock x:Name="QrCode2Text" Text="https://ligapervihpheniks.tilda.ws/"
                                           FontSize="15" Foreground="#4FC3F7"
                                           HorizontalAlignment="Center"
                                           TextWrapping="Wrap" TextAlignment="Center"/>
                            </Border>
                            <TextBlock Text="нажмите для копирования" FontSize="13"
                                       Foreground="#4A6A8A" HorizontalAlignment="Center"
                                       Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Информация о проекте -->
                <Border CornerRadius="18" Padding="32" Margin="0,0,0,24" BorderThickness="1.5">
                    <Border.Background>
                        <SolidColorBrush Color="#0F2341"/>
                    </Border.Background>
                    <Border.BorderBrush>
                        <SolidColorBrush Color="#1E4DB7"/>
                    </Border.BorderBrush>
                    <Border.Effect>
                        <DropShadowEffect Color="#000" Opacity="0.3" BlurRadius="16" ShadowDepth="0"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Информация о проекте" FontSize="22" FontWeight="Bold"
                                   Foreground="#4FC3F7" Margin="0,0,0,20"/>
                        <TextBlock x:Name="DeveloperText"
                                   Text="Здесь будет информация о разработчиках проекта..."
                                   FontSize="18" Foreground="#C8D8EA"
                                   TextWrapping="Wrap" LineHeight="30" Margin="0,0,0,20"/>
                        <Border Background="#122840" CornerRadius="10" Padding="20,14"
                                BorderThickness="1">
                            <Border.BorderBrush>
                                <SolidColorBrush Color="#2A5298"/>
                            </Border.BorderBrush>
                            <TextBlock Text="📬 Для связи: телеграмм: @RENDERGAMESRU"
                                       FontSize="16" Foreground="#8AADD4" FontWeight="Medium"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Техническая информация -->
                <Border CornerRadius="18" Padding="32" BorderThickness="1.5">
                    <Border.Background>
                        <SolidColorBrush Color="#0F2341"/>
                    </Border.Background>
                    <Border.BorderBrush>
                        <SolidColorBrush Color="#1E4DB7"/>
                    </Border.BorderBrush>
                    <Border.Effect>
                        <DropShadowEffect Color="#000" Opacity="0.3" BlurRadius="16" ShadowDepth="0"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Техническая информация" FontSize="22" FontWeight="Bold"
                                   Foreground="#4FC3F7" Margin="0,0,0,20"/>
                        <Border Background="#122840" CornerRadius="10" Padding="16,12"
                                Margin="0,0,0,8" BorderThickness="1">
                            <Border.BorderBrush><SolidColorBrush Color="#1E4DB7"/></Border.BorderBrush>
                            <TextBlock Text="• Платформа: .NET 8 (self-contained)"
                                       FontSize="17" Foreground="#C8D8EA"/>
                        </Border>
                        <Border Background="#122840" CornerRadius="10" Padding="16,12"
                                Margin="0,0,0,8" BorderThickness="1">
                            <Border.BorderBrush><SolidColorBrush Color="#1E4DB7"/></Border.BorderBrush>
                            <TextBlock Text="• Технология: WPF"
                                       FontSize="17" Foreground="#C8D8EA"/>
                        </Border>
                        <Border Background="#122840" CornerRadius="10" Padding="16,12"
                                Margin="0,0,0,8" BorderThickness="1">
                            <Border.BorderBrush><SolidColorBrush Color="#1E4DB7"/></Border.BorderBrush>
                            <TextBlock Text="• Совместимость: Windows 10/11"
                                       FontSize="17" Foreground="#C8D8EA"/>
                        </Border>
                        <Border Background="#122840" CornerRadius="10" Padding="16,12"
                                BorderThickness="1">
                            <Border.BorderBrush><SolidColorBrush Color="#1E4DB7"/></Border.BorderBrush>
                            <TextBlock Text="• WebView2"
                                       FontSize="17" Foreground="#C8D8EA"/>
                        </Border>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- Статусбар -->
        <Border Grid.Row="2" Padding="28,10" Background="#0A1628">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="VersionText"
                           Grid.Column="0"
                           Foreground="#2A4A6A" FontSize="13"
                           VerticalAlignment="Center"
                           FontFamily="Segoe UI, sans-serif"/>
                <TextBlock Grid.Column="1"
                           Text="© RENDERGAMES • Школьный киоск"
                           Foreground="#2A4A6A" FontSize="13"
                           VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

    </Grid>
</Window>
