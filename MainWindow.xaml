<Window x:Class="Kiosk.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Школьное расписание" 
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="White"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <!-- Анимации для баннеров -->
        <Storyboard x:Key="BannerFadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                     From="0" To="1" Duration="0:0:0.5"/>
        </Storyboard>

        <Storyboard x:Key="BannerFadeOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                     From="1" To="0" Duration="0:0:0.5"/>
        </Storyboard>

        <Storyboard x:Key="ImageFadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                     From="0" To="1" Duration="0:0:0.5"/>
        </Storyboard>

        <Storyboard x:Key="ImageFadeOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                     From="1" To="0" Duration="0:0:0.3"/>
        </Storyboard>

        <!-- Ресурсы для теней -->
        <DropShadowEffect x:Key="ShadowLight"
                          BlurRadius="15"
                          ShadowDepth="3"
                          Opacity="0.15"
                          Color="Black"/>

        <DropShadowEffect x:Key="ShadowMedium"
                          BlurRadius="20"
                          ShadowDepth="5"
                          Opacity="0.2"
                          Color="Black"/>

        <DropShadowEffect x:Key="ShadowStrong"
                          BlurRadius="25"
                          ShadowDepth="8"
                          Opacity="0.25"
                          Color="Black"/>

        <DropShadowEffect x:Key="ShadowButton"
                          BlurRadius="12"
                          ShadowDepth="3"
                          Opacity="0.2"
                          Color="Black"/>

        <DropShadowEffect x:Key="ShadowButtonHover"
                          BlurRadius="20"
                          ShadowDepth="6"
                          Opacity="0.3"
                          Color="Black"/>

        <!-- Градиенты для правой панели -->
        <LinearGradientBrush x:Key="AssistantHeaderGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#2c3e50" Offset="0"/>
            <GradientStop Color="#34495e" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="AssistantPanelGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#1a252f" Offset="0"/>
            <GradientStop Color="#2c3e50" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="AssistantContentGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#ecf0f1" Offset="0"/>
            <GradientStop Color="#bdc3c7" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="AssistantComboBoxGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="White" Offset="0"/>
            <GradientStop Color="#ecf0f1" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="AssistantInfoCardGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#3498db" Offset="0"/>
            <GradientStop Color="#2980b9" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="AssistantWarningGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#e74c3c" Offset="0"/>
            <GradientStop Color="#c0392b" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="AssistantSuccessGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#2ecc71" Offset="0"/>
            <GradientStop Color="#27ae60" Offset="1"/>
        </LinearGradientBrush>

        <!-- Стили для текста -->
        <Style x:Key="Heading1" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2c5f9e"/>
        </Style>

        <Style x:Key="Heading2" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2c5f9e"/>
        </Style>

        <Style x:Key="BodyText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="Normal"/>
        </Style>

        <Style x:Key="CaptionText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#6c757d"/>
        </Style>

        <!-- Обновленный стиль для кнопок с анимированными градиентами -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="10"
                                Effect="{StaticResource ShadowButton}">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#2c5f9e" Offset="0"/>
                                    <GradientStop Color="#2c5f9e" Offset="0.5"/>
                                    <GradientStop Color="#1a4a7a" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              Margin="20,15"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowButtonHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowLight}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Обновленный стиль для кнопки Новости с анимацией -->
        <Style x:Key="NewsButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="10"
                                Effect="{StaticResource ShadowButton}">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#4361ee" Offset="0"/>
                                    <GradientStop x:Name="newsGradient1" Color="#4361ee" Offset="0.5"/>
                                    <GradientStop x:Name="newsGradient2" Color="#3a0ca3" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              Margin="20,15"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowButtonHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowLight}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для ComboBox в правой панели -->
        <Style x:Key="AssistantComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource AssistantComboBoxGradient}"/>
            <Setter Property="Foreground" Value="#2c3e50"/>
            <Setter Property="BorderBrush" Value="#bdc3c7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"
                                    SnapsToDevicePixels="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="20"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                                      Margin="{TemplateBinding Padding}"
                                                      VerticalAlignment="Center"/>
                                    <Path Grid.Column="1"
                                          Data="M0,0 L4,4 8,0 Z"
                                          Fill="#2c3e50"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          Margin="0,0,5,0"/>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3498db"/>
                                <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowLight}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для карточек информации в помощнике -->
        <Style x:Key="AssistantInfoCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource AssistantInfoCardGradient}"/>
            <Setter Property="BorderBrush" Value="#2980b9"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Effect" Value="{StaticResource ShadowLight}"/>
        </Style>

        <Style x:Key="AssistantWarningCardStyle" TargetType="Border" BasedOn="{StaticResource AssistantInfoCardStyle}">
            <Setter Property="Background" Value="{StaticResource AssistantWarningGradient}"/>
            <Setter Property="BorderBrush" Value="#c0392b"/>
        </Style>

        <Style x:Key="AssistantSuccessCardStyle" TargetType="Border" BasedOn="{StaticResource AssistantInfoCardStyle}">
            <Setter Property="Background" Value="{StaticResource AssistantSuccessGradient}"/>
            <Setter Property="BorderBrush" Value="#27ae60"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="316"/>
        </Grid.ColumnDefinitions>

        <!-- Левая часть - Основная информация -->
        <Grid Grid.Column="0" Background="#f8f9fa">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Шапка с иконкой настроек -->
            <Border Grid.Row="0" Background="#2c5f9e" Padding="20" Effect="{StaticResource ShadowMedium}">
                <Grid>
                    <TextBlock Text="Школьный информационный киоск" 
                               Foreground="White" 
                               FontSize="26" 
                               FontWeight="Bold"
                               FontFamily="Segoe UI"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               TextOptions.TextRenderingMode="ClearType"
                               TextOptions.TextFormattingMode="Display"/>

                    <!-- Иконка настроек в правом верхнем углу -->
                    <Button x:Name="SettingsButton"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Width="40"
                            Height="40"
                            Background="Transparent"
                            BorderThickness="0"
                            Click="SettingsButton_Click"
                            ToolTip="Настройки">
                        <Border Background="White" 
                                CornerRadius="8"
                                Padding="8"
                                Effect="{StaticResource ShadowLight}">
                            <Image Source="/Images/settings_emoji.png" 
                                   Width="20" 
                                   Height="20"/>
                        </Border>
                    </Button>
                </Grid>
            </Border>

            <!-- Основной контент -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="600">
                    <!-- Иконка -->
                    <Viewbox Width="100" Height="100" Margin="0,0,0,20" HorizontalAlignment="Center">
                        <Border Background="#2c5f9e" 
                                CornerRadius="20" 
                                Padding="20"
                                Effect="{StaticResource ShadowMedium}">
                            <Image Source="/Images/AIhelper.png" 
                                   Width="32" 
                                   Height="32" 
                                   VerticalAlignment="Center"/>
                        </Border>
                    </Viewbox>

                    <!-- Информация о школе -->
                    <Border Background="White" 
                            CornerRadius="15" 
                            Margin="20,10"
                            BorderThickness="1"
                            BorderBrush="#dee2e6"
                            Effect="{StaticResource ShadowMedium}">
                        <StackPanel Margin="40,30">
                            <TextBlock Text="Муниципальное бюджетное общеобразовательное учреждение"
                                       Foreground="#495057"
                                       FontSize="18"
                                       FontWeight="Medium"
                                       FontFamily="Segoe UI"
                                       TextAlignment="Center"
                                       Margin="0,0,0,8"
                                       TextOptions.TextRenderingMode="ClearType"/>

                            <TextBlock Foreground="#2c5f9e"
                                       FontSize="24"
                                       FontWeight="Bold"
                                       FontFamily="Segoe UI"
                                       TextAlignment="Center"
                                       Margin="0,0,0,8"
                                       TextOptions.TextFormattingMode="Display">
                                <Run Text="Средняя общеобразовательная школа"/>
                                <LineBreak/>
                                <Run Text="«Лига первых»" FontWeight="Black"/>
                            </TextBlock>

                            <TextBlock Text="г. Химки"
                                       Foreground="#6c757d"
                                       FontSize="16"
                                       FontWeight="Normal"
                                       FontFamily="Segoe UI"
                                       TextAlignment="Center"
                                       Margin="0,10,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Дата и время -->
                    <Border Background="White"
                            CornerRadius="12"
                            Margin="40,25"
                            Padding="20"
                            Effect="{StaticResource ShadowLight}">
                        <StackPanel>
                            <TextBlock x:Name="TimeText" 
                                       Text="00:00:00"
                                       Foreground="#2c5f9e"
                                       FontSize="40"
                                       FontWeight="Bold"
                                       FontFamily="Segoe UI"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,5"
                                       TextOptions.TextFormattingMode="Display"/>

                            <TextBlock x:Name="DateText" 
                                       Text="понедельник, 1 января 2024"
                                       Foreground="#6c757d"
                                       FontSize="20"
                                       FontWeight="Medium"
                                       FontFamily="Segoe UI"
                                       HorizontalAlignment="Center"
                                       Opacity="0.9"/>
                        </StackPanel>
                    </Border>

                    <!-- Кнопки навигации с анимированными градиентами -->
                    <Grid Margin="20,30">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Первая строка -->
                        <Button x:Name="ScheduleButton"
            Grid.Row="0" Grid.Column="0"
            Margin="10,10"
            MinWidth="220"
            Height="85"
            Click="ScheduleButton_Click"
            Foreground="White"
            FontSize="22"
            FontWeight="SemiBold"
            FontFamily="Segoe UI"
            BorderThickness="0"
            Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                        CornerRadius="12"
                        Effect="{StaticResource ShadowButton}">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"
                                      Margin="25,20"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowButtonHover}"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowLight}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Background="White" 
                    CornerRadius="8" 
                    Padding="6"
                    Margin="0,0,15,0">
                                    <Image Source="/Images/ring.png" 
                       Width="32" 
                       Height="32"/>
                                </Border>
                                <TextBlock Text="Расписание" 
                       VerticalAlignment="Center"
                       FontSize="22"
                       Foreground="White"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="ReplacementsButton"
            Grid.Row="0" Grid.Column="1"
            Margin="10,10"
            MinWidth="220"
            Height="85"
            Click="ReplacementsButton_Click"
            Foreground="White"
            FontSize="22"
            FontWeight="SemiBold"
            FontFamily="Segoe UI"
            BorderThickness="0"
            Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                        CornerRadius="12"
                        Effect="{StaticResource ShadowButton}">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"
                                      Margin="25,20"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowButtonHover}"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowLight}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Background="White" 
                    CornerRadius="8" 
                    Padding="6"
                    Margin="0,0,15,0">
                                    <Image Source="/Images/refresh_emoji.png" 
                       Width="32" 
                       Height="32"/>
                                </Border>
                                <TextBlock Text="Замены" 
                       VerticalAlignment="Center"
                       FontSize="22"
                       Foreground="White"/>
                            </StackPanel>
                        </Button>

                        <!-- Вторая строка -->
                        <Button x:Name="AboutButton"
            Grid.Row="1" Grid.Column="0"
            Margin="10,10"
            MinWidth="220"
            Height="85"
            Click="AboutButton_Click"
            Foreground="White"
            FontSize="22"
            FontWeight="SemiBold"
            FontFamily="Segoe UI"
            BorderThickness="0"
            Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                        CornerRadius="12"
                        Effect="{StaticResource ShadowButton}">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"
                                      Margin="25,20"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowButtonHover}"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowLight}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Background="White" 
                    CornerRadius="8" 
                    Padding="6"
                    Margin="0,0,15,0">
                                    <Image Source="/Images/Logo.png" 
                       Width="32" 
                       Height="32"/>
                                </Border>
                                <TextBlock Text="О проекте" 
                       VerticalAlignment="Center"
                       FontSize="22"
                       Foreground="White"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="MapBrowserButton"
            Grid.Row="1" Grid.Column="1"
            Margin="10,10"
            MinWidth="220"
            Height="85"
            Click="MapBrowserButton_Click"
            Foreground="White"
            FontSize="22"
            FontWeight="SemiBold"
            FontFamily="Segoe UI"
            BorderThickness="0"
            Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                        CornerRadius="12"
                        Effect="{StaticResource ShadowButton}">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"
                                      Margin="25,20"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowButtonHover}"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowLight}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Background="White" 
                    CornerRadius="8" 
                    Padding="6"
                    Margin="0,0,15,0">
                                    <Image Source="/Images/map_emoji.png" 
                       Width="32" 
                       Height="32"/>
                                </Border>
                                <TextBlock Text="Карта здания" 
                       VerticalAlignment="Center"
                       FontSize="22"
                       Foreground="White"/>
                            </StackPanel>
                        </Button>

                        <!-- Третья строка - кнопка "Новости" -->
                        <Button x:Name="News"
            Grid.Row="2" 
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Margin="10,15"
            Height="95"
            Click="NewsButton_Click"
            Foreground="White"
            FontSize="24"
            FontWeight="Bold"
            FontFamily="Segoe UI"
            BorderThickness="0"
            Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                        CornerRadius="12"
                        Effect="{StaticResource ShadowButton}">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"
                                      Margin="25,20"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowButtonHover}"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Effect" Value="{StaticResource ShadowLight}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <StackPanel Orientation="Horizontal" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center">
                                <Border Background="White" 
                    CornerRadius="8" 
                    Padding="6"
                    Margin="0,0,20,0">
                                    <Image Source="/Images/newsVK.png" 
                       Width="34" 
                       Height="34"/>
                                </Border>
                                <TextBlock Text="Новости" 
                       VerticalAlignment="Center"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="White"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <!-- Статус бар -->
            <Border Grid.Row="2" 
                    VerticalAlignment="Bottom" 
                    Background="#f8f9fa" 
                    BorderThickness="1,1,0,0"
                    BorderBrush="#dee2e6"
                    Effect="{StaticResource ShadowLight}">
                <TextBlock x:Name="StatusText" 
                           Text="Готов к работе • F11 - полноэкранный режим"
                           Foreground="#6c757d"
                           FontSize="14"
                           FontFamily="Segoe UI"
                           HorizontalAlignment="Center"
                           Margin="15,12"/>
            </Border>
        </Grid>

        <!-- Правая часть - ИИ помощник с улучшенным дизайном -->
        <Border Grid.Column="1" 
        Background="#1a252f" 
        BorderBrush="#2c3e50" 
        BorderThickness="1,0,0,0"
        Effect="{StaticResource ShadowMedium}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Заголовок помощника -->
                <Border Grid.Row="0" 
                Background="#2c3e50"
                Padding="20"
                Effect="{StaticResource ShadowLight}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <StackPanel>
                            <TextBlock Text="Школьный помощник" 
                               Foreground="White" 
                               FontSize="22" 
                               FontWeight="Bold"
                               FontFamily="Segoe UI"/>
                            <TextBlock Text="ИИ-ассистент" 
                               Foreground="#bdc3c7"
                               FontSize="12"
                               FontFamily="Segoe UI"
                               HorizontalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Выбор класса -->
                <Border Grid.Row="1" 
                Background="#2c3e50" 
                Padding="20"
                Margin="0"
                Effect="{StaticResource ShadowLight}">
                    <StackPanel>
                        <TextBlock Text="Выберите класс для информации:" 
                           Foreground="#ecf0f1" 
                           FontSize="16"
                           FontWeight="SemiBold"
                           FontFamily="Segoe UI"
                           Margin="0,0,0,12"/>
                        <Border Background="White"
                        CornerRadius="8"
                        Effect="{StaticResource ShadowLight}"
                        Padding="2">
                            <ComboBox x:Name="AIClassComboBox" 
                              FontSize="14"
                              FontFamily="Segoe UI"
                              Background="Transparent"
                              Foreground="#2c3e50"
                              Padding="10,8"
                              BorderThickness="0"
                              SelectionChanged="AIClassComboBox_SelectionChanged">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="White" 
                                        Margin="1"
                                        Padding="8,6">
                                            <TextBlock Text="{Binding}" 
                                               FontFamily="Segoe UI"
                                               FontWeight="Medium"
                                               Foreground="#2c3e50"/>
                                        </Border>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Информация от ИИ -->
                <Border Grid.Row="2" 
                Margin="15" 
                CornerRadius="10"
                Background="#2c3e50"
                Effect="{StaticResource ShadowMedium}"
                BorderBrush="#34495e"
                BorderThickness="1">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="0">
                        <StackPanel x:Name="AIContentPanel" Margin="15">
                            <!-- Стартовое сообщение -->
                            <Border Background="#34495e"
                            CornerRadius="8"
                            Padding="15"
                            Margin="0,0,0,10"
                            BorderBrush="#4a6572"
                            BorderThickness="1"
                            Effect="{StaticResource ShadowLight}">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" 
                                        Margin="0,0,0,10">
                                        <Border Background="#3498db" 
                                        CornerRadius="6" 
                                        Padding="6"
                                        Margin="0,0,10,0">
                                            <Image Source="/Images/AIhelper.png" 
                                           Width="18" 
                                           Height="18"/>
                                        </Border>
                                        <TextBlock Text="Добро пожаловать!" 
                                           Foreground="White"
                                           FontSize="16"
                                           FontWeight="Bold"/>
                                    </StackPanel>
                                    <TextBlock Text="Выберите класс выше, чтобы увидеть актуальную информацию о расписании, заменах и важных объявлениях для вашего класса."
                                       Foreground="#ecf0f1"
                                       FontSize="14"
                                       FontFamily="Segoe UI"
                                       TextWrapping="Wrap"
                                       LineHeight="20"/>
                                </StackPanel>
                            </Border>

                            <!-- Здесь будет добавляться динамический контент -->
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>

        <!-- Режим баннеров -->
        <Grid x:Name="BannerGrid" 
      Grid.Column="0" 
      Grid.ColumnSpan="2"
      Visibility="Collapsed"
      Background="Black"
      Opacity="0">

            <!-- Фон с размытием и затемнением -->
            <Grid.Effect>
                <BlurEffect x:Name="BannerBlurEffect" Radius="0" />
            </Grid.Effect>

            <!-- Контейнер для плавных переходов -->
            <Grid x:Name="BannerContentGrid">
                <!-- Баннер с анимацией перехода -->
                <Grid x:Name="BannerContainer">
                    <Grid.Resources>
                        <Storyboard x:Key="BannerFadeIn">
                            <DoubleAnimation Storyboard.TargetName="BannerImage"
                                     Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:1"/>
                            <DoubleAnimation Storyboard.TargetName="BannerGrid"
                                     Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.5"/>
                        </Storyboard>
                        <Storyboard x:Key="BannerFadeOut">
                            <DoubleAnimation Storyboard.TargetName="BannerImage"
                                     Storyboard.TargetProperty="Opacity"
                                     From="1" To="0" Duration="0:0:0.5"/>
                        </Storyboard>
                    </Grid.Resources>

                    <!-- Изображение баннера с растяжением на весь экран -->
                    <Image x:Name="BannerImage" 
                   Stretch="UniformToFill"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   Opacity="0"/>
                </Grid>

                <!-- Кнопка выхода с размытым фоном -->
                <Grid VerticalAlignment="Bottom" Margin="0,0,0,40">
                    <Grid.Effect>
                        <DropShadowEffect BlurRadius="20" 
                                  Opacity="0.5" 
                                  ShadowDepth="0"
                                  Color="Black"/>
                    </Grid.Effect>

                    <!-- Размытый фон кнопки -->
                    <Border x:Name="ExitButtonBackground"
                    Background="#A0000000"
                    CornerRadius="15"
                    Margin="100,0"
                    Opacity="0.8">
                        <Border.Effect>
                            <BlurEffect Radius="10"/>
                        </Border.Effect>
                    </Border>

                    <!-- Сама кнопка -->
                    <Button x:Name="ExitBannerButton"
                    Content="Нажмите в любом месте экрана для выхода из режима баннеров"
                    Background="Transparent"
                    Foreground="White"
                    FontSize="24"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Height="80"
                    Margin="100,0"
                    Click="ExitBannerButton_Click"
                    Cursor="Hand"
                    Padding="20">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <ContentPresenter Content="{TemplateBinding Content}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>